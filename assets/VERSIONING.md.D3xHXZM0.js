import{At as e,Pt as t,Ut as n,gt as r,ht as i,mt as a,n as o,pt as s,ut as c}from"./chunks/framework.BrKcMR_c.js";var l=JSON.parse(`{"title":"Release identity and candidate preparation","description":"","frontmatter":{},"headers":[],"relativePath":"VERSIONING.md","filePath":"VERSIONING.md","lastUpdated":1789092097000}`),u={name:`VERSIONING.md`};function d(o,l,u,d,f,p){let m=t(`Term`);return e(),s(`div`,null,[l[15]||=c(`h1`,{id:`release-identity-and-candidate-preparation`,tabindex:`-1`},[i(`Release identity and candidate preparation `),c(`a`,{class:`header-anchor`,href:`#release-identity-and-candidate-preparation`,"aria-label":`Permalink to “Release identity and candidate preparation”`},`​`)],-1),l[16]||=c(`p`,null,[i(`Implemented by ADR-0040. Public release approval, clean-machine acceptance and performance qualification are still separate gates; see `),c(`a`,{href:`/RELEASE-CHECKLIST`},`release readiness`),i(`.`)],-1),c(`p`,null,[l[1]||=c(`a`,{href:`https://github.com/goatsoft/GOAT/blob/main/apps/goat-macos/release.json`,target:`_blank`,rel:`noreferrer`},`release.json`,-1),l[2]||=i(` is the editable product identity: canonical `,-1),l[3]||=c(`code`,null,`version`,-1),l[4]||=i(`, `,-1),l[5]||=c(`code`,null,`codename`,-1),l[6]||=i(` and positive integer `,-1),l[7]||=c(`code`,null,`build`,-1),l[8]||=i(`. Read that record for the current build number, which must remain above the legacy build `,-1),r(m,{term:`1337`},{default:n(()=>[...l[0]||=[i(`1337`,-1)]]),_:1}),l[9]||=i(`. Increment the build when preparing a new candidate with changed content. The record alone does not make a candidate distributable.`,-1)]),c(`p`,null,[l[13]||=i(`The current maintenance release is `,-1),c(`strong`,null,[l[11]||=i(`0.1.1 (`,-1),r(m,{term:`kid`},{default:n(()=>[...l[10]||=[i(`Kid`,-1)]]),_:1}),l[12]||=i(`)`,-1)]),l[14]||=i(`. The published 0.1.0 artifacts retain their original identity.`,-1)]),l[17]||=a(`<table tabindex="0"><thead><tr><th>Surface</th><th>0.1.1 release value</th></tr></thead><tbody><tr><td>About and Activity Log, either presentation mode</td><td><code>0.1.1 (Kid)</code></td></tr><tr><td>Website label and GitHub draft title</td><td><code>GOAT 0.1.1 (Kid)</code></td></tr><tr><td>Bundle version / tag / DMG filename</td><td><code>0.1.1</code> / <code>v0.1.1</code> / <code>GOAT-0.1.1.dmg</code></td></tr><tr><td>Secondary UI diagnostics</td><td><code>Development · build &lt;number&gt;</code> or <code>Candidate · build &lt;number&gt;</code></td></tr></tbody></table><p>Only a zero patch is omitted from display; <code>0.1.1 (Kid)</code> keeps its patch. Choose codenames from <a href="/CODENAMES">CODENAMES.md</a>. Patch candidates preserve their release-line codename. Versions change for releases, not milestones or rebuilds.</p><h2 id="build-and-validation" tabindex="-1">Build and validation <a class="header-anchor" href="#build-and-validation" aria-label="Permalink to “Build and validation”">​</a></h2><p><code>make gen</code> validates the record and generates <code>.build/release-settings.yml</code>, which XcodeGen includes. The tracked generated Info.plist contains build-setting references. Missing metadata, malformed versions and conflicting <code>MARKETING_VERSION</code>, <code>CURRENT_PROJECT_VERSION</code> or <code>GOAT_CODENAME</code> environment/Make overrides fail. Run <code>make gen</code> before opening the generated Xcode project. Direct edits or overrides in Xcode are not a supported release path; bundle validation catches drift before packaging.</p><p>Normal builds use <strong>Development</strong>. <code>make release</code> and <code>make dmg</code> use <strong>Candidate</strong> even with Release optimisation. A <strong>Release</strong> channel requires an explicit matching <code>RELEASE_TAG</code>, an exact canonical tag pointing to HEAD, and clean source. About and Log always disclose the channel of unpublished builds; missing UI metadata is shown as unknown rather than falling back to Kid.</p><p>Bundle metadata records HEAD, a SHA-256 of the working source and whether it was dirty. The source fingerprint includes tracked and nonignored untracked files, file contents and executable bits, excluding generated <code>App/Info.plist</code>. Ignored build output, dependency caches and local data are not hashed. This binds packaging to the working source, including uncommitted candidates; it is not a reproducible-build or supply-chain attestation.</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark-dimmed" style="--shiki-light:#24292e;--shiki-dark:#adbac7;--shiki-light-bg:#fff;--shiki-dark-bg:#22272e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#F69D50;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> verify</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> CONFIG=Release</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> SWIFT_FLAGS=&#39;-c release&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#6CB6FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> web</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#F69D50;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#F69D50;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> docs:build</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#6CB6FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> ..</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#F69D50;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> dmg</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> DIST=/absolute/path/to/a/new/candidate-directory</span></span></code></pre></div><p>For acceptance while the normal app is running, use a separate derived-data directory, for example <code>DERIVED=/path/to/separate-derived-data</code>, on both Make commands. These commands build and launch test hosts; they do not use <code>make run</code> or restart the normal app. Never restart GOAT during active chat or command work.</p><p>Packaging validates the original app, staged app and app inside a read-only mounted DMG against the current record and source fingerprint. It checks signatures, arm64 app/CLI architecture, CLI byte agreement, bundled and disk-image licence text, and the Applications shortcut. The filename comes from the record; no <code>dev</code> fallback or independent filename version exists. Existing candidate outputs are refused: choose a fresh <code>DIST</code> directory. A generated <code>release-metadata.json</code> records identity, channel, source and artifact hashes; <code>SHA256SUMS.txt</code> covers the DMG and manifest.</p><p>The <a href="https://github.com/goatsoft/GOAT/blob/main/apps/goat-macos/art/dmg/README.md" target="_blank" rel="noreferrer">Finder installer theme</a> renders the ImageGen artwork at 720 x 480 points and fills its blue capsule from the validated app&#39;s actual build number. Packaging needs a logged-in Finder session with Automation permission. The DMG includes <code>CLI Tools/goat</code> and a <code>Licence</code> folder, and verifies the saved window, background and image metadata after read-only remount. Smaller support folders, including <code>.background</code> when hidden files are visible, sit along the bottom. Existing published releases keep their original contents.</p><p>Increment <code>build</code> for a new candidate with changed content; keep it stable for a rebuild of the same candidate. The validator checks progression against the last different checked-in record and the legacy floor. For a previously distributed candidate, also provide its saved manifest:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark-dimmed" style="--shiki-light:#24292e;--shiki-dark:#adbac7;--shiki-light-bg:#fff;--shiki-dark-bg:#22272e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#F69D50;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> apps/goat-macos/scripts/release-metadata.py</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> check</span><span style="--shiki-light:#005CC5;--shiki-dark:#6CB6FF;"> --channel</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> Candidate</span><span style="--shiki-light:#005CC5;--shiki-dark:#F47067;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#6CB6FF;">  --previous</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;"> /path/to/previous/release-metadata.json</span></span></code></pre></div><p>The distributed manifest is necessary to distinguish a new candidate from a rebuild when the release record itself has not changed. Git history alone cannot establish which local artifacts were shared. Confirm there were no privately distributed builds above 1337 before externally distributing this first managed candidate.</p><h2 id="tagged-draft-workflow" tabindex="-1">Tagged draft workflow <a class="header-anchor" href="#tagged-draft-workflow" aria-label="Permalink to “Tagged draft workflow”">​</a></h2><p>After acceptance and explicit release authorization, the tag-triggered workflow:</p><ol><li>Validates the source, exact tag, macOS/SDK host and previous GitHub release manifests, including drafts. Existing tags owned by a release, regressing builds/versions, changed patch codenames and absent legacy manifests fail closed.</li><li>Runs lint, release-script regressions, optimised package tests, app tests, a normal Release build, and website/docs builds on the tagged commit.</li><li>Builds/signs the app and CLI, packages and remounts the DMG, requires notarization and stapling, and regenerates the manifest/checksums after stapling.</li><li>Uses <code>gh release create --draft --verify-tag</code>, with the derived title and explicit assets. It never uploads replacement assets to an existing release. Workflow runs are serialized rather than cancelled.</li></ol><p>The website reads its displayed label from the release record. <code>web/publication.json</code> controls availability separately. With <code>releaseTag</code> null, the main action leads to setup documentation. Set the exact approved <code>v&lt;version&gt;</code> tag and its codename only at launch; downloads then point to that tag’s artifact, with no runtime release lookup. Keep the previous publication record while preparing a new candidate so its existing download and label remain available. <code>web/package.json</code> is checked against the record as a compatibility mirror.</p><p>See <a href="/Releasing">Releasing</a> for signing and runner setup. A CI definition is not evidence that remote CI ran. Draft creation is not publication approval, notarization is not clean-machine acceptance, and ordinary tests do not complete runtime performance qualification.</p>`,18)])}var f=o(u,[[`render`,d]]);export{l as __pageData,f as default};