import{At as e,Pt as t,Ut as n,gt as r,ht as i,mt as a,n as o,pt as s,ut as c}from"./chunks/framework.BrKcMR_c.js";var l=JSON.parse(`{"title":"ADR-0023: One active turn and revisioned engine lifecycle","description":"","frontmatter":{},"headers":[],"relativePath":"adrs/0023-single-active-turn-and-engine-lifecycle.md","filePath":"adrs/0023-single-active-turn-and-engine-lifecycle.md","lastUpdated":1789092097000}`),u={name:`adrs/0023-single-active-turn-and-engine-lifecycle.md`};function d(o,l,u,d,f,p){let m=t(`Term`);return e(),s(`div`,null,[l[35]||=a(`<h1 id="adr-0023-one-active-turn-and-revisioned-engine-lifecycle" tabindex="-1">ADR-0023: One active turn and revisioned engine lifecycle <a class="header-anchor" href="#adr-0023-one-active-turn-and-revisioned-engine-lifecycle" aria-label="Permalink to “ADR-0023: One active turn and revisioned engine lifecycle”">​</a></h1><p><strong>Status:</strong> Accepted · 2026-08-31 · Extends <a href="/adrs/0002-ui-architecture">ADR-0002</a>, <a href="/adrs/0006-mcp-integration">ADR-0006</a>, <a href="/adrs/0016-chat-content-pipeline">ADR-0016</a>, and <a href="/adrs/0021-engines-as-managed-list">ADR-0021</a>; gates M6 (<a href="/adrs/0005-memory-architecture">ADR-0005</a>)</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to “Context”">​</a></h2>`,3),c(`p`,null,[l[1]||=i(`GOAT has one live inference client, one permission surface, and one `,-1),r(m,{term:`shepherd`},{default:n(()=>[...l[0]||=[i(`Shepherd`,-1)]]),_:1}),l[2]||=i(`, but chats expose independent send controls. A per-chat `,-1),l[3]||=c(`code`,null,`isStreaming`,-1),l[4]||=i(` flag does not prevent two chats from starting together, and async attachment preparation leaves a window where the same chat can start twice. A second start can replace the task handle for the first turn while both tasks continue to mutate transcripts, show permission prompts, persist messages, and use the shared engine.`,-1)]),c(`p`,null,[r(m,{term:`engine`},{default:n(()=>[...l[5]||=[i(`Engine`,-1)]]),_:1}),l[7]||=i(` discovery, health refresh, profile edits, key changes, and active-engine switches also cross suspension points. An older operation can finish after a newer one and commit stale health, `,-1),r(m,{term:`model`},{default:n(()=>[...l[6]||=[i(`models`,-1)]]),_:1}),l[8]||=i(`, endpoint, or credentials. Actor and `,-1),l[9]||=c(`code`,null,`@MainActor`,-1),l[10]||=i(` isolation prevent data races, but they do not make a multi-step async operation current after an `,-1),l[11]||=c(`code`,null,`await`,-1),l[12]||=i(`.`,-1)]),c(`p`,null,[l[15]||=i(`M6 adds memory `,-1),r(m,{term:`context-window`},{default:n(()=>[...l[13]||=[i(`context`,-1)]]),_:1}),l[16]||=i(` assembly, builtin `,-1),r(m,{term:`tool`},{default:n(()=>[...l[14]||=[i(`tools`,-1)]]),_:1}),l[17]||=i(`, and optional background reflection. Adding those jobs before turn ownership and engine lifecycle are explicit would multiply the same races and put first-token latency at risk.`,-1)]),l[36]||=a(`<h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to “Decision”">​</a></h2><h3 id="one-user-turn-app-wide-for-mvp" tabindex="-1">One user turn app-wide for MVP <a class="header-anchor" href="#one-user-turn-app-wide-for-mvp" aria-label="Permalink to “One user turn app-wide for MVP”">​</a></h3><ul><li>GOAT admits at most <strong>one user turn across the whole app</strong>. The turn covers preparation, streaming, tool permission and invocation rounds, scheduling final persistence, optional auto-title, and teardown.</li><li>Admission is synchronous and happens before attachment writes or any other <code>await</code>. A successful admission creates an immutable <code>TurnID</code> and records its <code>sessionID</code>. A second send is rejected as busy. It never implicitly cancels the existing turn.</li><li>Navigation remains available while a turn runs. Send and Regenerate entry points reject other work until the owner finishes. The app-wide Stop command targets the owning turn even if the user has navigated to another chat; a chat-local Stop targets only that chat.</li><li><code>session.isStreaming</code> mirrors turn ownership for presentation, but it is not the authority for admission. The Shepherd&#39;s app-wide reservation is the source of truth.</li><li>Auto-title currently runs inside the admitted turn, after the visible response and before teardown, so it cannot overlap another turn. M6 background generation adopts a lower-priority gate as specified below rather than copying this inline behavior.</li></ul><h3 id="identity-and-cancellation" tabindex="-1">Identity and cancellation <a class="header-anchor" href="#identity-and-cancellation" aria-label="Permalink to “Identity and cancellation”">​</a></h3>`,4),c(`ul`,null,[l[23]||=c(`li`,null,[i(`The reservation carries `),c(`code`,null,`TurnID`),i(` and `),c(`code`,null,`sessionID`),i(`. The turn task retains that reservation across suspension points, and teardown validates both values before clearing shared ownership or presentation state.`)],-1),l[24]||=c(`li`,null,[i(`Stop dismisses the owner's pending permission request and requests cancellation of its turn task. If generation has not started, Stop releases the preparation reservation immediately. A late preparation callback validates its `),c(`code`,null,`TurnID`),i(`; on lost ownership it persists the submitted text, discards attachment files which were never admitted, and cannot start a newer turn. Once generation has started, ownership remains held until guarded teardown, so a new turn cannot start while cancellation unwinds. `),c(`code`,null,`Task.cancel()`),i(` is a request, not proof that work has stopped.`)],-1),c(`li`,null,[l[19]||=i(`MVP has no separate persisted `,-1),l[20]||=c(`code`,null,`stopped`,-1),l[21]||=i(` outcome. Partial assistant output follows the normal flush and persistence path when the `,-1),r(m,{term:`streaming`},{default:n(()=>[...l[18]||=[i(`stream`,-1)]]),_:1}),l[22]||=i(` ends; cancellation-specific product state requires a schema decision outside this ADR.`,-1)]),l[25]||=c(`li`,null,[i(`Teardown clears the reservation, task handle, and session presentation state only when the finishing `),c(`code`,null,`TurnID`),i(` and `),c(`code`,null,`sessionID`),i(` still own them. A late preparation task therefore cannot clear a newer reservation.`)],-1),l[26]||=c(`li`,null,`Tool calls already dispatched may have external side effects. The loop checks identity and cancellation after schema discovery, permission, and invocation suspension points; late results are ignored, and undispatched tool events are persisted as stopped. Stop does not claim to undo completed external work.`,-1)]),l[37]||=a(`<h3 id="revisioned-engine-lifecycle-commits" tabindex="-1">Revisioned engine lifecycle commits <a class="header-anchor" href="#revisioned-engine-lifecycle-commits" aria-label="Permalink to “Revisioned engine lifecycle commits”">​</a></h3><ul><li>The MainActor records each user lifecycle intent synchronously with a monotonically increasing intent revision before scheduling async work. <code>EngineLifecycleController</code> accepts only increasing intent revisions and issues an operation token, so an older caller that reaches the actor late cannot supersede newer intent. Each operation captures its target profile ID and complete <code>EngineConfig</code>.</li><li>Discovery probes and Add/Edit tests use isolated clients. They do not repeatedly reconfigure the live engine while searching candidates.</li><li>An async lifecycle result may publish endpoint, profile URL, health, models, or default-model state only if its operation is still current and its target profile remains active. A superseded probe returns no resolution.</li><li>A current discovery resolution updates the live engine once with the operation revision, then revalidates both intent and operation before publishing observable state. The live engine rejects lower revision commits at its own actor boundary. Candidate probes never mutate the live engine, so a failed discovery cannot leave it pointed at the last port tried. While the current lifecycle intent is pending, turn admission is disabled.</li><li>Active-profile switches, active-profile edits, key changes, and live discovery are rejected while a user turn owns the engine. Read-only isolated Add/Edit tests remain allowed.</li><li>A turn is stable because app-owned engine mutations are blocked for the life of its reservation and the inference stream snapshots its <code>EngineConfig</code> when the request starts. The turn does not carry an engine-revision field.</li></ul><h3 id="m6-admission-gate" tabindex="-1">M6 admission gate <a class="header-anchor" href="#m6-admission-gate" aria-label="Permalink to “M6 admission gate”">​</a></h3><p>M6 implementation does not join the app until all of the following are true. These are forward gates, not a claim that Step 1 alone already satisfies every item:</p>`,4),c(`ol`,null,[l[31]||=c(`li`,null,[i(`Tests prove global single-turn admission, including two immediate sends before attachment preparation finishes and sends from different chats. Preparation carries its `),c(`code`,null,`TurnID`),i(`, and a stopped preparation cannot start a later reservation for the same session.`)],-1),l[32]||=c(`li`,null,`Tests prove stop/late-event identity: a cancelled or superseded task cannot append, persist, present permission UI, or clear a newer owner.`,-1),l[33]||=c(`li`,null,`Tests prove stale discovery, refresh, key, edit, and switch results cannot commit over a newer engine revision.`,-1),c(`li`,null,[r(m,{term:`memory`},{default:n(()=>[...l[27]||=[i(`Memory`,-1)]]),_:1}),l[28]||=i(` context is captured once per admitted user turn and reused across its tool rounds. Wiki reads and writes are actor-serialized and stay off `,-1),l[29]||=c(`code`,null,`MainActor`,-1),l[30]||=i(`.`,-1)]),l[34]||=c(`li`,null,`Auto-reflect and other model-backed memory work use the lower-priority engine gate, are cancellable, and cannot delay or overlap an admitted user turn.`,-1)]),l[38]||=a(`<h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to “Consequences”">​</a></h2><ul><li>Transcript ownership, permission UI, persistence, and Stop semantics become deterministic across chats.</li><li>Engine state reflects the newest lifecycle operation that has begun, not whichever network request happens to finish last. Engine-changing entry points mark the app as transitioning before scheduling that operation, so no turn is admitted in the handoff window.</li><li>One-at-a-time generation leaves potential multi-chat throughput on the table. This is accepted for MVP because GOAT has one active engine, one model selection, and one permission surface. Concurrency can be reconsidered only with independent per-turn engine sessions and a new ADR.</li><li>Turn IDs, engine revisions, guarded commits, and explicit teardown add code and tests. They replace timing assumptions with invariants that M6 can safely build on.</li><li>M6 background conveniences such as reflection may be skipped under user load. Responsiveness and correctness take priority over maintenance work; the current inline auto-title remains part of its owning turn.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to “Alternatives considered”">​</a></h2><p>Per-chat concurrent turns (rejected for MVP: shared engine lifecycle, permission UI, and task ownership are not independent), relying on <code>@MainActor</code> or actor serialization alone (rejected: actors re-enter across <code>await</code> and do not reject stale commits), cancelling the current turn whenever another chat sends (rejected: surprising and unsafe around tool side effects), and last-writer-wins health/discovery state (rejected: completion order is not user intent).</p>`,4)])}var f=o(u,[[`render`,d]]);export{l as __pageData,f as default};